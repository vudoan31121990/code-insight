const cplusplus = `if (condition) {
    // body of if statement
} else {
    // body of else statement
}`;

const csharp = `if (condition) {
    // body of if statement
} else {
    // body of else statement
}`;

const javascript = `if (condition) {
    // body of if statement
} else {
    // body of else statement
}`;

const python = `if condition:
    # body of if statement
else:
    # body of else statement
`;

const ruby = `if condition
    # body of if statement
else
    # body of else statement
end`;

const java = `if (condition) {
    // body of if statement
} else {
    // body of else statement
}`;

const swift = `if condition {
    // body of if statement
} else {
    // body of else statement
}`;

const go = `if condition {
    // body of if statement
} else {
    // body of else statement
}`;

const typescript = `if (condition) {
    // body of if statement
} else {
    // body of else statement
}`;

const php = `if ($condition) {
    // body of if statement
} else {
    // body of else statement
}`;

const ifelseCodeData = [
	{
		codeSnippetId: 'C++',
		code: cplusplus
	},
	{
		codeSnippetId: 'C#',
		code: csharp
	},
	{
		codeSnippetId: 'JavaScript',
		code: javascript
	},
	{
		codeSnippetId: 'Python',
		code: python
	},
	{
		codeSnippetId: 'Ruby',
		code: ruby
	},
	{
		codeSnippetId: 'Java',
		code: java
	},
	{
		codeSnippetId: 'Swift',
		code: swift
	},
	{
		codeSnippetId: 'Go',
		code: go
	},
	{
		codeSnippetId: 'TypeScript',
		code: typescript
	},
	{
		codeSnippetId: 'PHP',
		code: php
	}
];

module.exports = { ifelseCodeData };
